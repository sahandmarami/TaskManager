# Add project specific ProGuard rules here.
# Minification is disabled for this project (isMinifyEnabled = false),
# so these rules are kept minimal and for future use.

-keepattributes *Annotation*, InnerClasses
-dontnote kotlinx.serialization.**
